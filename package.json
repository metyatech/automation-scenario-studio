{
  "name": "@metyatech/automation-scenario-studio",
  "version": "0.1.0",
  "description": "CLI entrypoint for running Robot Framework suites and generating guidebook artifacts",
  "type": "module",
  "bin": {
    "automation-scenario": "dist/src/cli.js"
  },
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist",
    "python"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "scan:deps": "npm audit --audit-level=high",
    "verify": "npm run format && npm run lint && npm run typecheck && npm test && npm run build && npm run scan:deps",
    "run": "tsx src/cli.ts"
  },
  "dependencies": {
    "@metyatech/automation-scenario-renderer": "git+https://github.com/metyatech/automation-scenario-renderer.git#5eab0bca0330303c7d0bac1631a4ab55461bed39"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "eslint": "^9.36.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
